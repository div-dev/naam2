import { Text } from "react-native";
import { View } from "react-native";

export function Cam({ navigation }){
    return(
        <View>
            <Text>This is a Camera</Text>
        </View>
    )
}